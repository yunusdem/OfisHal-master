@model IList<VohalAramaDokum>
<style>
    table.dataTable.table-sm .sorting:before, table.dataTable.table-sm .sorting_asc:before, table.dataTable.table-sm .sorting_desc:before {
        top: 3px;
        right: 8px;
    }

    table.dataTable.table-sm .sorting:after, table.dataTable.table-sm .sorting_asc:after, table.dataTable.table-sm .sorting_desc:after {
        top: 9px;
    }

    .dt-button {
        margin: 0 !important;
        padding: 2px 5px !important;
    }

    .dt-buttons button:first-child {
        border-radius: 5px 0 0 5px !important;
    }

    .dt-buttons button:last-child {
        border-radius: 0 5px 5px 0 !important;
    }
</style>
<div class="table-responsive">
    <table class="display table table-hover table-sm mt-0" id="dokumTable" style="width:100%;">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Döküm No</th>
                <th scope="col">Marka</th>
                <th scope="col">Müstahsil</th>
                <th scope="col">Giriþ Tarihi</th>
                <th scope="col">Ýrsaliye No</th>
            </tr>
        </thead>
        <tbody class="table-group-divider">
            @for (var i = 0; i < Model.Count; i++)
            {
                var item = Model.ElementAtOrDefault(i);
                <tr class="rows" ondblclick="closeModal()">
                    <th scope="row">
                        @(i + 1)
                        <input type="radio" name="dokum" data-id="@item.MakbuzId" class="form-check-input ms-1" />
                    </th>
                    <td>@item.DokumNo</td>
                    <td>@item.Marka</td>
                    <td>@item.CariAdi</td>
                    <td>@item.StokGirisTarihi.ToString("dd.MM.yyyy")</td>
                    <td>@item.IrsaliyeNo</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<script>
    $(document).on("click", "tbody .rows", function (e) {
        $(this).find("input").prop("checked", true);
    });
    $(document).ready(function () {
        CreateDatatable('#dokumTable');
    });
    $(document).on("click", "#modalSaveBtn", function (e) {
        closeModal();
    });
    function closeModal() {
        var item = $("#dokumTable").find("input:checked");
        sessionStorage.setItem("makbuzId", item.data("id"));
        $("#modal").modal("hide");
    }
</script>